<h1 class="user-email"><%= @user.first_name %></h1>
<h2><%= @user.last_name %></h2>
<h2><%= @user.email %></h2>
<h2>This is your motorbikes</h2>

<div class="container mt-3">
  <%  @user_motorbikes.each do |motorbike| %>
    <div class="col-4" class="motorbike-index-card-div" >
          <%# <%= link_to motorbike_path(motorbike) do %>
            <div class="card">
              <%= cl_image_tag motorbike.photo.key, class: "moto-photo" %>
                <div class="motorbike-card-content">
                  <div class="motorbike-card-title">
                    <h5 class="card-title"><%= motorbike.name %></h5>
                  </div>
                  <p class="card-text"><%= motorbike.description %></p>
                  <div class="flex">
                    <div class="flex-align-items-center">
                      <p class="card-text"><%= motorbike.price %> € /day</p>
                    </div>
                  </div>
                </div>
                <div class="motorbike-card-actions">
                  <%= link_to "More info", motorbike_path(motorbike), class: "card-link" %>
                  <%# <%= link_to "Edit", edit_motorbike_path(motorbike), class: "btn btn-secondary" %>
                  <%# <%= link_to "Remove", motorbike_path(motorbike), data: { turbo_method: :delete, turbo_confirm: "Are you sure ?" }, class: "btn btn-danger" %>
                </div>
            </div>
          <%# <% end %>
        </div>
  <% end %>
</div>
